# api

示例 RPC 接口和示例数据对象。

## 主要文件

- `Add`: 示例服务接口，包含加法、减法和用户对象合并方法。
- `User`: 示例 DTO，用于演示复杂对象序列化和远程传输。
- `fallbackimpl`: 消费者侧示例降级实现。

这个包更像 demo API，用来验证框架能力。真实业务项目接入时，会把自己的服务接口放在业务模块中，再由消费者和服务端共同依赖。
