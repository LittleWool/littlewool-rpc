# message

RPC 协议消息模型。

## 主要文件

- `Message`: 请求和响应的通用外层消息。
- `Request`: RPC 请求体，包含接口、方法、参数、超时等调用信息。
- `Response`: RPC 响应体，包含返回值、异常或状态。
- `HeartbeatRequest`: 心跳请求。
- `HeartbeatResponse`: 心跳响应。

## 设计要点

`codec` 层负责把这些消息对象编码到网络字节流。修改字段时，需要同步检查：

- 编码器和解码器。
- 序列化兼容性。
- 消费者代理和服务端反射调用。
- 单元测试里的协议回归用例。
